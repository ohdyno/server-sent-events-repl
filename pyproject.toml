[project]
name = "server-sent-events-repl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.121.0",
    "uvicorn>=0.38.0",
]
